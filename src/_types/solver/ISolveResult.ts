import {IVariableCollection} from "./IVariableCollection";

/** Either the variable collection or undefined if the formula can't be solved */
export type ISolveResult = undefined | IVariableCollection;
